# Docker Compose Environment Variables
# Copy this file to .env.local for local development

# API URL for backend connection
# For Docker Desktop (Windows/Mac)
VITE_API_URL=http://host.docker.internal:8000

# For Linux Docker (alternative option)
# VITE_API_URL=http://172.17.0.1:8000

# For use with external backend server
# VITE_API_URL=http://your-backend-host:8000

# Port for frontend container (default 3000)
FRONTEND_PORT=3000

# Examples for different environments:
# Development: VITE_API_URL=http://localhost:8000
# Docker: VITE_API_URL=http://host.docker.internal:8000
# Production: VITE_API_URL=https://your-api-domain.com
