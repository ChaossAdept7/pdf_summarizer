# PDF Summarizer

A PDF summarization application with a FastAPI backend and React frontend.

## Getting Started

### Frontend
To run the frontend application, navigate to the `frontend` directory and follow the instructions in its README.

### Backend
To run the backend API, navigate to the `backend` directory and follow the instructions in its README.

## Future Enhancements

- Database connection for storing summaries and user data
